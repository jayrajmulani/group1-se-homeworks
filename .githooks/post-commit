#!/bin/sh
echo "executing post-commit"
echo 'Git commit successful. Generating documentation'
homedir=$(git rev-parse --show-toplevel)
cd $homedir
cd docs/code
python -m pdoc ../code/* -o .
cd $homedir
cd docs/tests
python -m pdoc ../tests/* -o .
echo 'Regenerated documentation files'